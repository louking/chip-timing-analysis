[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "chip-timing-analysis"
version = "0.2.0"
requires-python = ">=3.10"
dependencies = ["pandas"]

[tool.setuptools.packages.find]
where = ["src"]
